catalog_version: "1.0"
profiles:
  - profile_id: P0
    tls_group: X25519
    endpoint_authentication_mode: classical_x509
    contract_evidence_mode: mldsa65
    fallback_rule: low_risk_only
    resumption_rule: context_bound
    lease_strictness: long
    max_lease_seconds: 3600
    assurance:
      key_establishment_threats:
        - classical
      endpoint_authentication_threats:
        - classical
      contract_evidence_threats:
        - classical
        - quantum
      fallback_rule: low_risk_only
      resumption_rule: context_bound
      lease_strictness: long
    resource_envelope:
      max_handshake_bytes: null
      max_peak_memory_kib: null
      max_cpu_time_microseconds: null
      max_energy_proxy_units: null
    description: >
      Classical X25519 key establishment with classical TLS endpoint authentication.
      ML-DSA-65 is represented only as application-level evidence for manifests and
      contracts. Hybrid or ML-KEM key establishment does not automatically imply fully
      post-quantum endpoint authentication; this initial catalog explicitly keeps endpoint
      TLS authentication classical.
  - profile_id: P1
    tls_group: X25519MLKEM768
    endpoint_authentication_mode: classical_x509
    contract_evidence_mode: mldsa65
    fallback_rule: explicit_only
    resumption_rule: contract_bound
    lease_strictness: medium
    max_lease_seconds: 900
    assurance:
      key_establishment_threats:
        - classical
        - quantum
      endpoint_authentication_threats:
        - classical
      contract_evidence_threats:
        - classical
        - quantum
      fallback_rule: explicit_only
      resumption_rule: contract_bound
      lease_strictness: medium
    resource_envelope:
      max_handshake_bytes: null
      max_peak_memory_kib: null
      max_cpu_time_microseconds: null
      max_energy_proxy_units: null
    description: >
      X25519MLKEM768 key establishment with classical TLS endpoint authentication.
      ML-DSA-65 is represented only as application-level evidence for manifests and
      contracts. Hybrid or ML-KEM key establishment does not automatically imply fully
      post-quantum endpoint authentication; this initial catalog explicitly keeps endpoint
      TLS authentication classical.
  - profile_id: P2
    tls_group: SecP256r1MLKEM768
    endpoint_authentication_mode: classical_x509
    contract_evidence_mode: mldsa65
    fallback_rule: explicit_only
    resumption_rule: contract_bound
    lease_strictness: medium
    max_lease_seconds: 900
    assurance:
      key_establishment_threats:
        - classical
        - quantum
      endpoint_authentication_threats:
        - classical
      contract_evidence_threats:
        - classical
        - quantum
      fallback_rule: explicit_only
      resumption_rule: contract_bound
      lease_strictness: medium
    resource_envelope:
      max_handshake_bytes: null
      max_peak_memory_kib: null
      max_cpu_time_microseconds: null
      max_energy_proxy_units: null
    description: >
      SecP256r1MLKEM768 key establishment with the same assurance and policy class as P1.
      ML-DSA-65 is represented only as application-level evidence for manifests and
      contracts. Hybrid or ML-KEM key establishment does not automatically imply fully
      post-quantum endpoint authentication; this initial catalog explicitly keeps endpoint
      TLS authentication classical.
  - profile_id: P3
    tls_group: MLKEM768
    endpoint_authentication_mode: classical_x509
    contract_evidence_mode: mldsa65
    fallback_rule: forbidden
    resumption_rule: contract_bound
    lease_strictness: medium
    max_lease_seconds: 900
    assurance:
      key_establishment_threats:
        - classical
        - quantum
      endpoint_authentication_threats:
        - classical
      contract_evidence_threats:
        - classical
        - quantum
      fallback_rule: forbidden
      resumption_rule: contract_bound
      lease_strictness: medium
    resource_envelope:
      max_handshake_bytes: null
      max_peak_memory_kib: null
      max_cpu_time_microseconds: null
      max_energy_proxy_units: null
    description: >
      MLKEM768 key establishment with classical TLS endpoint authentication.
      ML-DSA-65 is represented only as application-level evidence for manifests and
      contracts. Hybrid or ML-KEM key establishment does not automatically imply fully
      post-quantum endpoint authentication; this initial catalog explicitly keeps endpoint
      TLS authentication classical.
  - profile_id: P4
    tls_group: SecP384r1MLKEM1024
    endpoint_authentication_mode: classical_x509
    contract_evidence_mode: mldsa87
    fallback_rule: forbidden
    resumption_rule: forbidden
    lease_strictness: short
    max_lease_seconds: 120
    assurance:
      key_establishment_threats:
        - classical
        - quantum
      endpoint_authentication_threats:
        - classical
      contract_evidence_threats:
        - classical
        - quantum
      fallback_rule: forbidden
      resumption_rule: forbidden
      lease_strictness: short
    resource_envelope:
      max_handshake_bytes: null
      max_peak_memory_kib: null
      max_cpu_time_microseconds: null
      max_energy_proxy_units: null
    description: >
      SecP384r1MLKEM1024 key establishment with classical TLS endpoint authentication.
      ML-DSA-87 is represented only as application-level evidence for manifests and
      contracts. Hybrid or ML-KEM key establishment does not automatically imply fully
      post-quantum endpoint authentication; this initial catalog explicitly keeps endpoint
      TLS authentication classical.
